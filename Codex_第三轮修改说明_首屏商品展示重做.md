# Codex 第三轮修改指令｜重做首屏商品展示（移动端优先）

本轮不要新增后台、登录、数据采集或其他新功能。只集中解决首屏商品展示的问题。

当前版本的商品“轮盘”技术上能运行，但视觉效果不符合目标：它更像电商 Banner 的轮播组件，而不是一个让中亚小商户快速理解商品与价格差异的比价展示页。

## 一、修改目标

将当前“多商品轮盘”调整为更克制、更稳定的**主商品比价展示器**：

> 先清楚看见一件熟悉商品 → 再看当地零售价和中国采购参考价 → 用户主动切换其他商品。

用户进入页面第一眼应该先理解商品和两种价格，不需要先理解轮盘动画。

## 二、当前问题

### 1. 商品图片太小，且视觉中心不稳定

当前中央商品图片被下方的商品名称和规格挤压。部分商品因为图片自身留白不同，看起来没有处于展示框真正中间的位置。

### 2. 首屏信息太拥挤

当前同时存在：

- 大标题；
- 中央商品；
- 左右虚化商品；
- 多行商品名称；
- 多行规格；
- 两条价格轨道；
- 箭头；
- 圆点；
- 自动轮播/轨道动画感。

这些元素同时抢注意力，用户无法第一秒聚焦“这是什么商品、两种价格分别是多少”。

### 3. 主标题占据屏幕过多

在约 360px 宽的手机上，Kyrgyz 标题常常有三行且字体过大，用户进入页面后需要继续下滑才能真正看清商品和价格。

### 4. 反向轮盘动画不是用户价值

商品轨道和价格轨道反向运动虽然实现了，但不应继续作为主要视觉特色。我们要的是清楚、可信、稳定的商品—价格对应关系，不是复杂动画。

## 三、核心改法：轮盘改为“主商品展示器”

### 移动端默认状态

- 只突出显示**一个当前商品**；
- 默认不自动轮播；
- 左右商品预览建议删除，或仅保留极弱的边缘提示；
- 底部圆点建议删除，改成简单进度，例如 `1 / 5`；
- 用户通过左右箭头或横滑切换商品；
- 商品切换时，商品图、名称、规格、当地价格、中国价格必须作为同一状态同步更新。

不要继续强化“三条轨道反向移动”的效果。

## 四、商品图片区必须重构

### 1. 图片与文字必须彻底分区

目前商品图、名称、规格在同一区域内，长标题会挤压图片。请改为固定结构：

```text
┌────────────────────────────┐
│                            │
│        商品图片展示框        │
│      （仅负责图片居中）      │
│                            │
└────────────────────────────┘
          商品名称
          商品规格
```

要求：

- 图片展示框只负责展示图片，内部不得放名称、规格、价格或按钮；
- 商品名称和规格必须放在图片框外部；
- 商品名称/规格变长时，不能压缩图片空间；
- 切换不同商品时，图片展示框高度保持不变；
- 商品信息区也要预留稳定高度，避免切换商品时页面上下跳动。

### 2. 确保商品视觉上真正居中

移动端建议：

- 商品图片展示框高度提高到约 `220px–250px`；
- 中央商品视觉尺寸比当前至少放大约 **25%**；
- 图片使用：

```css
object-fit: contain;
object-position: center;
```

- 图片外层使用 flex 居中：

```css
display: flex;
align-items: center;
justify-content: center;
```

注意：部分商品原图自带的留白不同，会让商品主体看起来偏上或偏下。请检查至少前五个 Hero 商品；如确有必要，可以在产品数据中支持单个图片的 `objectPosition` 或轻微视觉偏移配置。不要用一条全局偏移规则硬套全部商品。

### 3. 限制商品文案高度

移动端：

- 商品名称最多两行；
- 商品规格最多两行；
- 超出使用省略，不允许无限撑高；
- 商品信息不应遮挡、挤压或抢占图片框；
- 商品名称字号应清晰，但明显弱于主标题。

## 五、压缩移动端首屏标题

请专门优化约 `360px` 和 `390px` 宽度下的首屏：

- 移动端 `h1` 字号下调约 **10–15%**；
- 收紧标题上下 margin/padding；
- 调整最大宽度和断行，避免不必要的三行大标题；
- 副标题控制在两行以内；
- 减少 Hero 顶部和底部空白。

**验收目标：** 用户打开首屏时，无需明显下滑，就应能看到：

1. 标题；
2. 当前商品大图；
3. 商品名称；
4. 当地零售价；
5. 中国采购参考价。

## 六、价格区调整原则

保留当前双价格对照的基本方向：

- “当地零售价”与“中国采购参考价”继续使用明显不同的颜色；
- 两张价格卡必须始终属于当前展示的同一个 `productId`；
- 卡片高度和价格位置固定，切换商品时不要上下跳；
- 继续保留“价格仅供选品初步参考”的免责声明；
- 不得使用“利润”“暴利”“稳赚”“赚多少”等营销表达。

但不要让两条价格卡继续作为复杂反向动画轨道运行。正确绑定和易读性优先于动画。

## 七、交互与动效

### 保留

- 左右箭头切换；
- 手机横滑切换；
- 键盘操作；
- `aria-live` 对当前商品和价格做简洁播报；
- reduced-motion 模式下不自动动画。

### 调整

- 默认关闭自动轮播；
- 删除或明显弱化左右虚化商品；
- 删除圆点，替换为 `当前序号 / 总数`；
- 商品切换动画改为克制的淡入淡出或轻微横移；
- 动画建议 `250–350ms`；
- 不应出现图片先切换、价格后切换，或价格属于上一件商品的中间状态。

## 八、桌面端要求

桌面端可以保留少量左右预览，帮助表达“还有更多商品”，但：

- 中央商品必须明显最大、最清晰；
- 两侧商品只能作为辅助，不能抢主视觉；
- 商品图与名称/规格仍然必须分区；
- 默认也不要自动轮播；
- 不能因为桌面/移动布局不同而破坏商品与价格的 `productId` 绑定。

## 九、完成后必须提供

请完成后提供以下内容：

1. **360px 宽**移动端首屏截图；
2. **390px 宽**移动端首屏截图；
3. 桌面端首屏截图；
4. 5–10 秒的商品切换录屏或 GIF；
5. 以下命令的实际终端输出：

```bash
pnpm lint
pnpm build
pnpm test
```

## 十、最终验收标准

- 中央商品比当前明显更大，视觉上稳稳处于图片展示框中心；
- 商品名称/规格再长，也不会影响图片大小和位置；
- 360px 手机首屏中，商品和两种价格能快速进入视野；
- 页面默认不自行切换商品；
- 每次切换后，图片、名称、规格、两种价格严格属于同一件商品；
- 页面不再有强烈的“多轨轮盘/电商 Banner”感觉；
- 整体感受应更接近：**可靠、清楚、克制的选品比价工具**。
