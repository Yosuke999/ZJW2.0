# 中亚商机网：第一轮返工指令

这不是小修补。请基于当前项目进行一次聚焦返工，把它从“技术演示原型”改成“能让当地商贩快速看懂商品与价格对比的高保真前台原型”。

本轮只处理下列 P0/P1 问题；不要趁机加入后台、数据库、登录、真实采价、支付、筛选器或无关功能。完成后必须能稳定本地构建，并更新测试，使测试验证当前网站，而非遗留模板。

---

## 一、验收结论与返工目标

当前版的主要问题是：商品图实际上是 Unicode 符号而非商品图片；轮盘不是要求中的三层反向联动；多语言页仍固定显示中文品牌；咨询面板无法传递用户当前查看的商品；项目在当前目录无法完成构建，且测试仍是旧模板测试。

本轮完成标准：用户在手机上打开 `/kg` 或 `/uz` 后，3 秒内能看到并认出真实外观的熟悉商品、理解两种本币参考价格的对比；页面不呈现 SaaS、数据后台或虚假招商风格；代码可以从干净依赖安装状态构建和测试。

---

## P0-1：替换所有符号商品占位，使用清晰的白底商品视觉

### 当前问题

`data/products.ts` 定义了 `image: /products/{id}.webp`，但 `HeroCarousel.tsx` 和 `ProductGrid.tsx` 没有渲染该字段，仍展示 `item.symbol` / `product.symbol`，例如 `♨`、`▣`、`◉`。这不符合首屏“用户先认出熟悉商品”的核心需求。

### 必须修改

1. 为 20 件商品提供可用的白底商品图片资源。第一版可以使用无品牌、无平台水印、无虚构认证的 AI 占位图或自行制作的简洁 SVG/PNG 插画，但视觉必须像对应商品本身，不能再用 Unicode 字符、emoji、抽象符号或纯色图标代替。
2. 图片资源放在 `public/products/`，并确保每个 `products.ts` 内的 `image` 路径真实存在。
3. 在首屏及商品卡使用 `<img>` 或 Next Image 实际渲染 `product.image`；提供有意义的 `alt` 文本，例如当前页面语言的商品名称。
4. 维持白色/近白背景、轻微自然投影、统一画面比例；不要使用仓库、店铺、人物、钞票、金币、平台 Logo 等背景元素。
5. 保留 `imageStatus: 'placeholder'` 数据字段，并在 README 明确说明：图片为占位素材，公开上线前必须替换为自有或已获授权的真实商品图。
6. 页面不要在每张商品卡或商品图上反复贴“AI”“占位图”等干扰用户的标签；演示属性由页面的统一“演示数据 · 待核验”提示和 README 表明即可。

### 验收

- 网络请求或静态文件检查能确认 20 个图片路径存在。
- 手机端首屏中央商品一眼能看出是水壶、充电宝、灯泡、耳机或吹风机，而不需要阅读文字。
- 手机端商品网格每张卡都显示真实外观的商品视觉，不是字符。

---

## P0-2：重新实现首屏三层反向联动轮盘

### 当前问题

目前只有商品区切换，价格轨道只是同一价格文本的轻微整体位移；并没有“商品行、当地价格行、中国价格行”各自横向移动且在停止后对齐的轮盘。`PriceTrack.tsx` 中的 `10 × · DEMO` 也不适合普通用户阅读。

### 必须修改

轮盘必须始终以同一 `productId` 为唯一数据绑定键；不能按数组视觉位置临时组合商品与价格。

视觉实现可以采用“有限卡片轨道 + 循环索引”，不要求真的无限滚动，但需要清楚表现三层轨道的反向运动：

1. 第一层为商品轨道：中央完整突出一个商品，左右露出相邻商品；商品图、名称、关键规格属于这一层。
2. 第二层为当地参考零售价轨道：切换时与商品轨道反向移动。
3. 第三层为中国进货参考价轨道：切换时与第二层反向，因此与商品轨道同方向。
4. 动画时长约 0.7 秒；每个商品完成对齐后停留约 3 秒，再开始下一次切换。
5. 动画过程中价格行允许降低透明度，避免临时错位造成误读；动画结束时三行必须明确对齐到当前 `productId`。
6. 用户拖动或点击左右可见商品时，暂停自动轮换；松手后吸附到最近商品；约 6 秒无操作后恢复自动切换。
7. 可保留很克制的点状进度指示，但不要加入“左右滑动”文案、巨型箭头或教程动画。
8. 必须支持 `prefers-reduced-motion`：减少动态时停用自动运动，提供可用的点击切换或淡入淡出切换。
9. 删除首屏与价格轨道中的英文 `DEMO`、`10 ×` 技术标识。采购 10 件的口径只在下方价格说明里用当地语言说明。首屏保留清晰的本地化演示标识和确认日期即可。

### 验收

- 录像或手动检查时，三层切换的方向关系肉眼明确：商品/中国价同向、当地价反向。
- 无论自动切换、拖拽或点击，最终商品和两组价格始终来自同一 productId。
- 首屏视觉没有工程/调试词汇，例如 `DEMO`、`10 ×`、商品 ID。

---

## P0-3：按当前语言本地化品牌，而非固定中文

### 当前问题

`Header.tsx` 与 `Footer.tsx` 将文字硬编码为 `中亚商机网`。这会使 `/kg`、`/uz` 和俄语版看起来没有真正面向当地用户。

### 必须修改

1. 在 `data/translations.ts` 或独立的品牌数据文件中，添加 `brandName` 字段，并在 Header/Footer/metadata 中按当前语言使用。
2. 暂用工作稿品牌翻译：
   - 中文：`中亚商机网`
   - 吉尔吉斯语：`Борбор Азия бизнес мүмкүнчүлүктөрү порталы`
   - 乌兹别克语：`Markaziy Osiyo biznes imkoniyatlari portali`
   - 俄语：`Портал бизнес-возможностей Центральной Азии`
3. 这些语言版本均保持翻译状态为 `draft`，README 中说明上线前需人工语言审核。
4. 可以保留简洁图形品牌标记，但不能让 `CA` 方块成为比品牌名更突出的主视觉；不要把它做成典型 SaaS App logo。品牌排版应更接近可信、克制的信息服务入口。
5. 页面 `<html lang>` 应随路由语言设为 `ky`、`uz`、`ru` 或 `zh`，不能固定为 `zh`。

### 验收

- 打开 `/kg`、`/uz`、`/kg/ru`、`/uz/ru`，页头和页尾品牌名与当前语言一致。
- `html` 根节点语言正确，便于读屏和搜索引擎理解。

---

## P0-4：修正联系面板的商品上下文

### 当前问题

`PortalPage.tsx` 始终向 `ContactSheet` 传 `heroProducts[0].id`，即使用户正在看其他轮盘商品，或点击了商品卡，也会传水壶 ID。

### 必须修改

1. 在 `PortalPage` 中维护 `selectedProductId` 状态，默认可为当前首屏轮盘商品。
2. `HeroCarousel` 通过回调把当前活动 productId 传回父级；轮盘切换完成后应更新当前商品。
3. `ProductGrid` 的商品点击回调也应把该商品 ID 传回父级；本轮不要求打开详情页，但应能让后续咨询携带正确商品上下文。可以选择“点击商品即打开联系面板”，或“点击商品只选中，用户之后点咨询按钮”；无论选哪种，行为要可理解且不得误导。
4. 联系面板中 WhatsApp、Telegram 的预填内容使用当前页面语言，包含国家、商品名称、商品 ID；电话链接只拨号。
5. 不传姓名、手机号、设备信息或任何未经确认的个人数据。

### 验收

- 轮盘切到耳机后点击咨询，生成的联系上下文是耳机对应名称和 `tws-earbuds`，不是水壶。
- 点击任一商品卡再咨询时，联系上下文与该商品一致。

---

## P0-5：修复项目可复现构建与测试

### 当前问题

在项目目录运行 `npm run build` 失败：`Cannot find package 'vite' imported from ...vinext...`。检查显示 `node_modules/vite` 是失效空链接。现有 `tests/rendered-html.test.mjs` 仍在断言 “Your site is taking shape” 骨架，与当前项目无关。

### 必须修改

1. 选择一种并贯彻：
   - 若部署目标是 Vercel，优先采用标准 Next.js 构建方式（`next dev`、`next build`、`next start`），移除不必要的 vinext、Cloudflare Worker、D1 示例与模板基础设施；或
   - 若确实必须采用 vinext/Cloudflare，则修复 lockfile 与依赖安装流程，并在 README 清楚说明该选择及构建命令。

   第一版部署在 Vercel 的前提下，建议前者。不要为未启用的数据库、Worker 或 Cloudflare 兼容层保留复杂基础设施。

2. 删除/替换无关测试。至少添加与现有网站匹配的验证：
   - 路由 `/kg`、`/uz` 以及俄语/中文预览路径可解析；
   - 20 个商品数据项均有存在的图片资源；
   - 每个国家都有 20 个价格，货币与国家匹配；
   - Header/Footer 品牌按语言读取；
   - 首屏没有 `DEMO`、`10 ×` 等工程展示文本。
3. 不要提交 `node_modules`。应有可用 lockfile；在干净目录执行 `npm ci` 或与锁文件一致的安装命令后，`npm run build`、`npm run lint`、`npm test` 都必须通过。
4. 更新 README：项目用途、安装/运行/构建命令、全部路由、数据文件位置、占位图片与演示价格/联系方式不可直接公开上线的提醒、将来接入 Supabase 的替换点。

### 验收

请在干净安装后提供以下命令的无报错结果：

```bash
npm ci
npm run lint
npm run build
npm test
```

若因包管理器选择改用 pnpm，必须提交 `pnpm-lock.yaml` 并提供等价命令，不能混用失效的 npm lockfile 与 pnpm node_modules。

---

## P1-1：去除 SaaS/调试感，强化“商品货架”而不是“后台面板”

请在完成 P0 后再处理本项。

1. 保持深蓝 `#174A7E`、青绿 `#22A69A`、浅灰白背景的主方向，但减少“圆角大白卡 + 字符 Logo + 圆点控件”的模板感。
2. 首屏和产品区应通过清晰商品图、轻层板线、留白和克制阴影体现货架陈列层次；不要增加图表、指标、渐变大球、玻璃拟态、夸张阴影。
3. 价格是首要信息，但不能压过商品识别；中文进货参考价用青绿，其他文字保持深灰/深蓝。
4. 不使用金币、现金、收益曲线、利润数字、折扣爆炸标签、购物车、评分星级、倒计时等元素。
5. 联系面板中的 WhatsApp/Telegram/电话应更接近真实通讯入口，而不是显示带 `https://` 的技术 URL。演示状态可用本地化小字说明，但展示给用户的主内容应是渠道名和“演示联系方式”。

---

## P1-2：细节与无障碍修正

1. 页面主标题、按钮、FAQ、轮盘控制在键盘操作下均可聚焦和使用；焦点样式清楚。
2. 轮盘商品切换时 `aria-live` 不应造成过度播报；以当前商品信息为简洁播报即可。
3. 联系面板打开后应支持 Escape 关闭，并尽可能将键盘焦点限制在弹层内；关闭后焦点回到原触发按钮。
4. `src` 参数在页头/页尾语言切换后也应保留，当前页尾语言链接未保留该参数，请统一修正。
5. 确认小屏（约 360px）、常见手机宽度、平板和桌面端均没有横向溢出；长的吉尔吉斯语/俄语标题不能遮挡或截断。

---

## 不要做的事

- 不接真实平台价格、1688 链接、供应商链接、抓取、数据库或后台。
- 不捏造“已核验”“当地团队”“真实客户”“仓库”“办公室”“成交案例”或正式联系方式。
- 不把价格差称为利润、收益或节省金额。
- 不增加登录、注册、表单、支付、购物车、商品详情页或复杂筛选。
- 不用国旗作为国家/语言标识。
- 不为视觉效果牺牲低性能手机与减少动态设置的体验。

---

## 提交时请一并说明

请在最终回复中简明列出：已完成的 P0/P1 项；修改过的主要文件；实际执行并通过的安装、lint、build、test 命令；仍需要人工确认的事项（尤其是商品图片授权、三种目标语言校对、真实联系方式、真实价格）。
