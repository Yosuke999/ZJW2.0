# Codex 第四轮修改指令｜三层轮盘动画优化与商品图片完整展示

本轮继续**保留三层轮盘**。请不要再将其改回单商品静态展示器。

当前部署版已恢复商品图轨道、当地零售价轨道、中国采购参考价轨道，但还有两个未通过验收的问题：

1. 轮盘切换动画不够顺，视觉上会有跳变、抢位或“下一张突然变大”的感觉；
2. 商品图片在展示区内仍没有稳定、完整地展示出来，部分商品看起来被裁掉、被相邻元素干扰，或主体未真正位于框内中央。

本轮只解决这两个问题，不新增功能，不改动路由、语言、价格数据或联系流程。

## 一、不可改变的三层轮盘规则

切换到下一件商品时：

- 商品图轨道向左移动；
- 中国采购参考价轨道向左移动；
- 当地零售价轨道向右移动；
- 过渡结束后，三条轨道中央 slot 都必须是同一个 `productId`。

切换到上一件商品时，方向反转。

请继续使用五项窗口：

```text
[前二] [前一] [当前] [后一] [后二]
```

当前项始终是索引 `2`。商品图和中国价格用正序窗口，当地价格用反序窗口。

## 二、修复轮盘动画：只做“平移”，不要在运动中抢位或跳变

当前 CSS 在动画期间同时做了以下事情：

- 整条 `track-strip` 平移；
- 旧中央项缩小/淡出；
- 下一项突然改为 `scale(1)`、`opacity: 1`；
- 同时改变 z-index。

这会使动画读起来像“元素突然抢到中央位置”，而不是稳定的连续轮盘移动。请重做动画状态。

### 目标效果

用户点“下一件”时，应清楚看到：

```text
商品图轨道：       ←
中国采购价轨道：    ←
当地零售价轨道：    →
```

整个过程应连续、同步、没有闪烁、没有瞬间换图、没有价格先换或后换。

### 实施要求

1. 保持轨道位移是主动画：

```css
.track-strip {
  transition: transform 420ms cubic-bezier(.22,.72,.22,1);
}
```

2. 不要在 `data-shift` 状态下用 `nth-child()` 直接把下一项瞬间设置为最终的 `opacity: 1`、`transform: scale(1)` 或最高 `z-index`。

3. 如需中央项与侧项有大小/透明度差异，必须让这些变化和轨道平移具有同样的 `420ms` 过渡曲线；推荐采用明确的状态类，例如：

```text
current
leaving-left / leaving-right
entering-left / entering-right
side
```

而不是用动画期间的 `nth-child(2/3/4)` 强制覆盖样式。

4. 在动画开始时保留旧数据窗口和旧中央项；仅在 `TRANSITION_MS` 结束后才更新 `index`、重置 `direction`、生成下一组五项窗口。

5. 不得在切换开始的同一帧调用 `setIndex(nextIndex)`；否则 React 会立即换掉窗口内容，造成视觉跳变。

6. 三条轨道必须共享同一份：

```ts
const TRANSITION_MS = 420;
const direction: 1 | -1 | null;
```

动画结束时间必须一致。

7. 用户连续点击、连续横滑、或动画未结束时再次操作，必须被锁定或安全忽略，不能重叠触发多段动画。

### 三条轨道必须有同一套视觉节奏

当前商品轨道有图片缩放、标题显隐等额外变化，价格轨道主要只移动数字，三层虽然数据同步，但视觉上不像同一个联动结构。请统一三条轨道的节奏：

- 商品图、当地价、中国价必须同时开始和结束位移；
- 三者采用相同的时长与 easing；
- 相邻价格的透明度不要低到看不出它在移动；
- 商品标题/规格的淡入淡出如保留，也必须与轨道动画同步，不能比价格轨道更晚或更早完成；
- 三层之间保持稳定、清楚的垂直间距，不能在动画时发生重叠。

## 三、商品图片必须“完整可见、稳定居中、互不遮挡”

目前 `.product-visual` 固定为约 208px 宽，但一个轨道 item 在移动端实际可用宽度更窄；相邻商品的视觉框会越过 item 边界，导致中央图片容易和左右项相互干扰。仅使用 `object-fit: contain` 还不够。

### 1. 中央商品必须拥有独立且足够的图片展示框

请确保中央 slot 的图片框在 360px 宽移动端内，完整落在可见区域中；商品的任何部位不能被轮盘 viewport、相邻商品、标题或价格轨道遮住。

建议结构：

```text
商品图轨道 viewport
└─ 五项 track strip
   └─ 每个 stage item
      ├─ 图片安全框（固定尺寸，只放图片）
      └─ 文案安全区（固定高度，商品名/规格）
```

要求：

- 中央图片框要有稳定尺寸；
- 中央图片框宽度不得超过它在视窗内的安全可视宽度；
- 侧项可以被缩小，但中央项不能依赖“越过 item 边界”才显示完整；
- 商品图与相邻商品不能重叠覆盖；
- 所有 Hero 商品切换时，图片框尺寸和垂直位置保持不变；
- 商品名称/规格永远在图片框外的独立固定信息区，不能挤压图片框。

### 2. 图片适配规则

中央图片必须使用：

```css
object-fit: contain;
object-position: center;
max-width: 100%;
max-height: 100%;
```

图片展示框必须使用：

```css
display: flex;
align-items: center;
justify-content: center;
overflow: hidden;
```

但注意：`overflow: hidden` 只能用于裁掉图片框之外的内容，**不能用来掩盖中央商品本身超过安全框的问题**。

请检查前五个 Hero 商品：玻璃电热水壶、充电宝、LED 灯泡、蓝牙耳机、吹风机。

- 每张图片都必须从顶部到底部完整可见；
- 商品主体视觉中心应接近图片框几何中心；
- 若原图自身白边不一致，可在产品数据中为个别图片加 `objectPosition` 或小幅 `imageOffsetY` 配置；
- 不能通过全局 CSS 把所有商品统一上移/下移来解决单张图问题。

### 3. 推荐移动端尺寸原则

以 360px 宽为最低验收基准：

- 商品图片安全框高度：约 `220–240px`；
- 中央商品图片必须在安全框内完整可见；
- 中央商品图要明显大于两侧预览；
- 侧项只作为提示，缩放、透明度可以降低，但不能覆盖中央商品；
- 商品文字区预留固定高度，不能影响图片高度。

### 移动端安全宽度必须先计算再定尺寸

当前五项轨道总宽为视窗的约 `166.6667%`，每项为轨道宽度的 `20%`。因此在约 360px 的移动端中，单个轨道 item 的实际安全宽度远小于当前约 208px 的图片框。图片框超过 item 的安全宽度，是图片越界、中央视觉错位和相邻商品互相干扰的根本原因。

请按实际轨道几何关系重设尺寸，不要继续只靠调大/调小 `.product-visual` 来试错：

- 先计算移动端每个 item 的安全可视宽度；
- 中央图片框及其内容必须完全落在该安全范围内；
- 如保留大尺寸中央商品，则需改变轨道布局/中央层级，使中央展示框拥有独立安全空间；
- 不允许中央图片依赖越过 item 边界才得以完整显示；
- 验收时在 360px、390px、以及桌面宽度逐一检查，不得只按某一个宽度调好。

不要为了做大图片而裁掉壶嘴、插头、手柄、耳机盒边缘等商品轮廓。

### 商品名称与规格不应随五项轨道一起穿插

当前每个商品轨道 item 都携带商品名称和规格，切换时五组不同长度的文案会一起移动。多语言环境下这会使画面拥挤、文字互相干扰，也会影响图片区的视觉稳定性。

请调整为：

- 商品图片继续位于五项商品轨道中并随轮盘移动；
- 当前商品名称和规格移至商品轨道下方的**固定信息区**；
- 固定信息区只展示当前中央 `productId` 的文案；
- 在轨道切换期间，文案只做一次轻微同步淡入/淡出，不能随五项 item 横向穿插；
- 信息区固定高度，名称最多两行、规格最多两行；
- 图片轨道与固定信息区的状态必须在动画结束时对应同一个 `productId`。

## 四、价格轨道的视觉要求

价格轨道继续保持五项条带与反向运动。

- 价格中央项在轨道中央清楚可读；
- 相邻价格可较淡，但须随轨道连续移动；
- 不得突然由旧价格替换成新价格；
- 当前商品的当地价格与中国采购价格必须同步对应同一 `productId`；
- 三条轨道不要互相重叠，保持稳定垂直间距。

## 五、验收交付（必须）

完成后请提供：

1. 360px 宽移动端静态首屏截图；
2. 390px 宽移动端静态首屏截图；
3. 360px 宽下前五个 Hero 商品各一张截图，证明图片完整显示；
4. 5–10 秒真实录屏/GIF：包含一次“下一件”和一次“上一件”切换；
5. 录屏必须能清楚看出：商品图与中国采购价同向、当地零售价反向，且无跳变；
6. `pnpm lint`、`pnpm build`、`pnpm test` 的实际输出；
7. 补充/更新自动测试，至少验证：
   - 五项窗口正确；
   - 商品/中国/当地三轨方向正确；
   - 动画结束后中央 item 的 `productId` 在三条轨道一致；
   - 默认不自动轮播；
   - 连续快速触发切换时不会叠加多个动画或造成 index 跳跃；
   - 商品固定信息区在动画结束后与三条轨道中央 `productId` 一致。

注意：现有测试如果只是检查函数结果、CSS 文字或组件中是否存在某段代码，不能证明真实轮盘动画正确。请增加交互级验证，或至少以浏览器录屏逐项证明双向切换后中央商品、当地价、中国价严格同步。

本轮最终目标是：**三层轮盘仍然存在且能被感知，但动画像一个稳定、连续的机械结构；中央商品则永远完整、清楚、居中地展示。**
