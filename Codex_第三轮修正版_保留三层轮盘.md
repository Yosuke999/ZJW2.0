# Codex 第三轮修正版｜保留并重做三层轮盘效果

上一版“主商品展示器”的表述偏离了核心需求：**三层轮盘效果必须保留。**

本次不是取消轮盘，也不是把它改成静态商品卡；而是在保留三层联动、反向运动的前提下，修正移动端商品图小、居中不稳、文字挤压的问题。

## 一、不可删除的核心效果

首屏必须继续保留以下三层独立轨道，并且用户能明确看出它们在联动：

1. **商品图轨道**：展示当前商品及左右相邻商品；
2. **当地零售价轨道**：展示与当前商品绑定的当地零售价；
3. **中国采购参考价轨道**：展示与当前商品绑定的中国采购参考价。

当用户向前切换一件商品时：

- 商品图轨道向左移动；
- 中国采购参考价轨道向左移动；
- 当地零售价轨道向右移动；
- 动画结束后，三条轨道中央位置必须对应同一个 `productId`。

当用户向后切换时，上述方向反转。

**不能用单一商品图片加两张静态价格卡替代三层轮盘。** 当前实现已经将 `PriceTrack` 改为只渲染单个价格卡，这属于需求回退，必须恢复为真正的价格轨道。

## 二、这次真正要修的，不是轮盘本身

原先的问题是轮盘的移动体验和首屏视觉层级不够好，不是三层轮盘这个概念错误。

请保留三层轮盘，但按以下方式优化：

- 轮盘默认不自动播放，用户主动用箭头、横滑或键盘切换；
- 中间商品始终最大、最清楚；
- 左右商品仍要存在，用于表达“可连续浏览”，但在移动端缩小、淡化，不能抢中央商品；
- 底部可保留进度或圆点，但不要喧宾夺主；
- 动画仍需能让人看出三层的反向关系，但时长控制在 `350–500ms`，不要显得像跑马灯。

## 三、商品轨道重构要求

### 1. 图片框与文字分离，但图片仍属于商品轨道

每个商品轨道 item 应使用稳定结构：

```text
商品轨道 item
├─ 固定高度图片展示框（只放图片）
└─ 固定高度信息区（商品名 + 规格）
```

要求：

- 图片展示框必须是独立固定高度区域；
- 商品名和规格不能压缩、挤走图片；
- 中央商品图片展示框在移动端建议高 `220–250px`；
- 中央商品视觉尺寸较当前版本至少放大约 25%；
- 图片使用 `object-fit: contain` 和 `object-position: center`；
- 图片框使用 flex 水平、垂直居中；
- 商品名最多两行、规格最多两行；信息区预留固定高度，切换时页面不得明显上下跳动；
- 检查前五个 Hero 商品的源图留白。如视觉主体仍明显偏上或偏下，可在产品数据中为单个商品配置 `objectPosition` / `imageOffset`，不要用全局偏移强行修正。

### 2. 保持五项窗口和中央定位

商品轨道和两条价格轨道继续使用五项窗口：

```text
[前二] [前一] [当前] [后一] [后二]
```

- 当前商品固定在中间 slot（索引 `2`）；
- 商品图轨道与中国价格轨道使用相同顺序；
- 当地价格轨道使用反向顺序；
- 三条轨道需要以 `productId` 绑定，不能依赖单纯数组视觉位置。

## 四、价格轨道必须恢复为真正轨道

当前 `PriceTrack.tsx` 只接收一个 `product` 并输出一张 `price-card`，已不满足三层轮盘要求。

请恢复为接收一个产品窗口，例如：

```ts
products: Product[]
direction: 1 | -1 | null
tone: "local" | "china"
```

并渲染含五个价格 item 的横向 strip。

建议保留并验证以下逻辑：

```ts
getCarouselWindow(heroProducts, index)        // 商品图、中国价格
getCarouselWindow(heroProducts, index, true)  // 当地零售价
getTrackShift(direction, "product") === -1
getTrackShift(direction, "china") === -1
getTrackShift(direction, "local") === 1
```

价格卡应该随各自轨道移动，不应在切换时突然直接替换为新价格。动画完成后，三条轨道中心卡必须绑定同一商品。

## 五、移动端视觉优先级

在 360px 与 390px 宽度下：

- 主标题字号下调约 10–15%，减少首屏占高；
- 中央商品图必须大、清晰、视觉居中；
- 左右商品保留但只作为边缘预览，建议比中央商品小且透明度低；
- 商品图、当地零售价、中国采购参考价应在首屏快速可见；
- 三层轨道之间要有明确的垂直层级和适当间距，不能挤成一个拥堵区域；
- 不能为了图片居中而删除左右商品或删除两条价格轨道。

## 六、交互、可访问性与动效

必须保留：

- 左右箭头切换；
- 横滑切换；
- 键盘左右方向键；
- `aria-live` 播报当前商品与两种价格；
- `prefers-reduced-motion` 下停止自动轮播和大幅动画；
- 商品、当地价、中国价始终按同一个 `productId` 更新。

默认不自动轮播；用户首次进入时可看到完整三层结构，主动切换后才播放联动动画。

## 七、验收标准

完成后请自查并提交：

1. 360px 宽移动端首屏截图；
2. 390px 宽移动端首屏截图；
3. 桌面端首屏截图；
4. 5–10 秒录屏/GIF，必须清楚展示一次向前切换和一次向后切换；
5. 录屏中必须能看出：商品图与中国价格同向、当地价格反向；
6. `pnpm lint`、`pnpm build`、`pnpm test` 的实际输出；
7. 为轮盘逻辑补充/恢复测试，验证五项窗口、反向窗口、三轨方向和中央 `productId` 对齐。

最终视觉应是：**一个以当前商品为中心、三层价格联动清晰、移动端商品图稳稳居中的比价轮盘。**
