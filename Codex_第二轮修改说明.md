# 中亚商机网：第二轮修改说明

本轮基于已部署版本 `https://central-asia-opportunity-portal.vercel.app/kg` 的验收。第一轮 P0 已有明显完成：商品图已经真实渲染、品牌按语言变化、商品咨询上下文已修正、项目也已切回标准 Next.js 结构。

本轮不要重做页面信息架构，也不要添加后台、登录、数据库、真实采价、支付、商品详情页或复杂筛选。目标是把目前“正确但偏静态、偏模板”的版本，提升为真正符合核心创意的移动端商品机会页。

---

## 本轮最重要的判断

目前页面已经能展示商品、价格与服务说明，但首屏最关键的“三层反向联动价格轮盘”仍没有真正完成。

当前代码虽然同时显示了三条内容轨道，也在 CSS 中给本地价和中国价赋予不同 `data-direction`，但切换时实际动画仍是整块商品区和整块价格区的小幅平移；用户看不出三行独立的、彼此反向的横向运动。它看起来更像内容瞬间换掉后抖一下，而不是一个有商品、当地价、中国价三层联动的价格轮盘。

请将本轮绝大部分精力投入首屏，其他项目只做必要优化。

---

## P0：完成真正可见的三层反向联动价格轮盘

### 要达到的用户观感

用户应当一眼理解：上面是商品，下面两行分别是当地零售价与中国进货参考价；当用户拖动或页面自动切换时，商品行和中国价行向同一方向移动，当地价行向反方向移动，三行停止后又准确落到同一商品。

不要把它做成赌场轮盘、无休止跑马灯或高频炫技动效。它应该像一个有明确停顿的、可靠的价格比较装置。

### 实现要求

1. 将商品行、当地价格行、中国价格行改为真正独立的横向轨道，而不是仅给外层区块施加 `translateX`。

2. 每条轨道应渲染至少当前项与左右相邻项（推荐 3 项，必要时 5 项以避免边缘空白）。每项都带稳定的 `productId`，且商品、两类价格只能通过相同 `productId` 绑定。

3. 切换“下一件商品”时：
   - 商品轨道向左移动一个单元；
   - 中国进货参考价轨道向左移动一个单元；
   - 当地参考零售价轨道向右移动一个单元。

   切换“上一件商品”时方向完全相反。

4. 0.7 秒左右的动画完成后，再更新/循环索引，确保中央商品及两个中央价格都对应同一个 `productId`。不要在动画中直接先换文本再抖动。

5. 自动模式：商品居中停留约 3 秒，随后用约 0.7 秒切换；动画结束后再重新开始停留计时。不要用固定 `setInterval` 造成慢设备或连续交互时的重叠切换；更推荐在单次动画完成后排下一次切换。

6. 用户手势：
   - 触摸商品轨道时立刻暂停自动切换；
   - 水平拖动超过阈值切换到相邻商品；
   - 松手后吸附到最近一件商品；
   - 用户停止操作约 6 秒后恢复自动切换；
   - 纵向滑动页面不能被轮盘拦截。

7. 可以保留左右两侧半露出的商品，但商品图片应该比现在更有“陈列”感：中央商品完整突出，左右相邻商品缩小、降低不透明度。价格轨道也展示左右相邻价格，以清楚表达轨道存在。

8. 过渡时允许把两条价格行降低到约 35–55% 不透明度，避免用户在移动中把暂时错位的价格理解为最终组合；停止对齐后恢复清晰。

9. 减少动态设置：`prefers-reduced-motion` 下关闭自动轮换和轨道滑动动画。用户仍可使用可见的、低干扰的上一个/下一个按钮或点状导航切换，切换用无动画或淡入淡出。

10. 保持可访问性：当前中央商品可通过简洁的 `aria-live` 报告名称与规格；不要在每次价格轨道动画时触发冗长播报。键盘可使用上一件/下一件控制，且焦点样式可见。

### 禁止的伪实现

以下都不算完成：

- 只改变中央文字或图片，然后给整个容器轻微 `translateX`；
- 三行一起向同一个方向滑动；
- 用淡入淡出替代正常模式下的轨道方向关系；
- 动画期间商品与价格失去同一 `productId` 绑定；
- 依赖 CSS 延迟让视觉“看起来像”反向，但内容没有真实轨道位置。

### 验收方式

请录制或提供一个 8–12 秒的手机宽度 GIF/视频：自动从水壶切到充电宝，再由用户向相反方向拖回水壶。视频中必须清楚看到：商品/中国价同向、当地价反向，且最终三行中心都是同一商品。

---

## P1：首屏视觉进一步去模板化

目前商品图已正确接入，这是重要进步。下一步只做克制调整，不要推翻色彩和结构。

1. 弱化 `CA` 方形标记。它可以保留为极小辅助符号，但不应比长品牌名更抢眼，也不要让页面像 SaaS 管理工具。若保留，建议改成更轻的线性几何/字母组合，不用厚重实心圆角方块。

2. 首屏轮盘外框继续采用“淡货架层板”的表达：减少卡片感和圆角感。可以保留上下的细分隔线、极淡阴影，但不要形成一个强边框的大白色 App 面板。

3. 统一所有占位说明的措辞和层级。目前“演示数据 · 待核验”是正确的，但不必每一个视觉块都重复。保留在首屏标题上方、商品区标题附近和价格说明中即可；不要在价格数字旁添加英文、工程状态或商品 ID。

4. 商品卡可保持两列，但适当减少“圆角白卡”重量：图片区域和文字区域的层级可更像货架标签，而不是电商促销卡。不要加入星级、折扣、购物车、销量、倒计时、利润标签或“省多少钱”。

5. 尤其检查 360px 宽手机：吉尔吉斯语品牌较长，页头应不挤压语言按钮；必要时品牌允许两行或将标记缩小，但不要缩小到难以阅读。

---

## P1：明确商品点击行为，减少意外跳出感

当前商品卡点击会直接弹出联系面板。功能上可以工作，但用户可能只是想浏览商品，不应因随意点击而突然被迫进入咨询。

请改为以下任一方案，推荐方案 A：

A. 商品卡点击仅选中该商品并给出轻微、非打断式视觉反馈（例如商品卡边框/标签短暂高亮）；主要咨询按钮仍使用最近选中的商品上下文。可以在商品卡下方或标题附近增加一行很克制的提示，例如“已选择此商品，可在下方咨询”。

B. 商品卡点击打开一个简短的非模态商品信息层，层内有清楚的“咨询这个商品”按钮；只有用户点击该按钮才打开联系面板。

无论采用哪种方案，必须保证从轮盘或商品卡选中的商品 ID、当前语言、当前国家，都能正确传到 WhatsApp / Telegram 的预填内容中。电话只保留拨号链接。

---

## P1：修正 Telegram 和联系方式演示方式

1. 当前 Telegram 使用 `t.me/share/url` 分享页，且 URL 参数内放的是演示 Telegram 链接。这并不是“联系本地顾问”的直达入口。正式接入时应配置真实的 Telegram 用户名/深链；第一版可以使用不可用的演示深链，但结构要为直接联系做准备。

2. 将国家联系数据拆分得更明确，例如：

```ts
telegramHandle: "example_contact"
telegramUrl: "https://t.me/example_contact"
whatsappNumber: "00000000000"
whatsappUrl: "https://wa.me/00000000000"
phone: "+000 000 000 000"
```

3. WhatsApp 继续使用 `?text=` 预填文案。Telegram 若不能可靠预填消息，则直接打开 `telegramUrl`；不要伪装成已经给对方带上了商品上下文。可通过前端事件记录用户从哪个商品进入，但不要把用户数据传给第三方。

4. 联系面板中主视觉只显示渠道名和“演示联系方式”，不要展示 `https://` 或技术 URL；这一点目前已有改善，请保持。

---

## P1：发布前工程验证与清理

项目已经声明使用 pnpm，请在自己的可写本地工作区完成以下验证，并在提交说明中粘贴精简结果：

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm lint
pnpm build
pnpm test
```

补充要求：

1. 需要确认 `pnpm-lock.yaml` 与 `package.json` 一致；不要提交 `node_modules`。
2. 删除项目中已不使用的 Vite、vinext、Cloudflare Worker、D1、Drizzle、旧模板测试或生成目录；若它们已经删除，检查 `.gitignore` 是否覆盖 `.next`、`node_modules`、`dist` 等构建产物。
3. 在测试中增加或替换一项结构性检查：三条轨道渲染的项目均引用同一产品数据源，且本地价轨道的切换方向与商品/中国价相反。无需为 CSS 像素动画写脆弱快照测试，但不能只靠文字搜索断言。
4. 校验 `/kg`、`/kg/ru`、`/kg/zh`、`/uz`、`/uz/ru`、`/uz/zh` 的语言、品牌、货币、路由均正确；语言切换继续保留合法 `src` 参数。

---

## 保持不变的已通过项

以下内容已经朝正确方向完成，除非为本说明中的必要调整，不要回退：

- 使用 20 张白底、无品牌商品占位图，而非 Unicode/emoji 图标；
- 国家/语言路径与 `html lang` 的处理；
- 四种语言分别显示对应的工作稿品牌名；
- 商品、价格、翻译、联系人仍使用独立数据层；
- 不展示利润、收益、节省金额或夸张招商承诺；
- 商品区之后才显示价格口径说明、信任信息和首次咨询；
- 不使用注册、登录、咨询表单、支付和真实用户追踪；
- 联系面板支持 Escape 关闭、焦点管理和当前商品上下文。

---

## 最终交付内容

完成后请提供：

1. 更新后的部署链接；
2. 8–12 秒手机宽度轮盘演示 GIF 或视频；
3. 简短变更说明，按“轮盘 / 视觉 / 联系方式 / 工程验证”四项汇报；
4. 实际执行的 `pnpm lint`、`pnpm build`、`pnpm test` 结果；
5. 仍需人工处理的事项：商品图片授权替换、吉尔吉斯语/乌兹别克语/俄语人工审校、真实联系人与真实价格核验。
